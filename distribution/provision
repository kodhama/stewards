#!/usr/bin/env python3
"""Stewards bounded pre-agent provisioner (spec 0002)."""

from pathlib import Path
import sys


sys.path.insert(0, str(Path(__file__).resolve().parent))

from lib.provision import main  # noqa: E402


if __name__ == "__main__":
    raise SystemExit(main())
